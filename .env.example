# 复制为本文件名 .env 后按需填写。所有变量均可留空（使用默认值）。

# 临时邮箱后端：local=占位（不收信），接你自己的 MoeMail / Cloudflare Worker 时改为对应值
MAIL_BACKEND=local

# 注册用代理（http://user:pass@host:port），留空=直连
REG_PROXY=

# 接码平台 API Key（Grok / Codex 需要手机号验证时使用）
SMS_API_KEY=
SMS_COUNTRY=us

# 注册浏览器是否无头模式
HEADLESS=true
